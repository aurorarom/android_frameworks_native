diff a/libs/ui/GraphicBuffer.cpp b/libs/ui/GraphicBuffer.cpp	(rejected hunks)
@@ -51,6 +60,10 @@ GraphicBuffer::GraphicBuffer()
     format = 
     usage  = 0;
     handle = NULL;
+
+#ifdef MTK_AOSP_ENHANCEMENT 
+    RefBaseMonitor::getInstance().monitor(this);
+#endif
 }
 
 GraphicBuffer::GraphicBuffer(uint32_t w, uint32_t h, 
@@ -65,6 +78,10 @@ GraphicBuffer::GraphicBuffer(uint32_t w, uint32_t h,
     usage  = 0;
     handle = NULL;
     mInitCheck = initSize(w, h, reqFormat, reqUsage);
+
+#ifdef MTK_AOSP_ENHANCEMENT 
+    RefBaseMonitor::getInstance().monitor(this);
+#endif
 }
 
 GraphicBuffer::GraphicBuffer(uint32_t w, uint32_t h,
