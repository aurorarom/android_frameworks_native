diff a/services/surfaceflinger/DisplayDevice.cpp b/services/surfaceflinger/DisplayDevice.cpp	(rejected hunks)
@@ -114,12 +119,29 @@ DisplayDevice::DisplayDevice(
     // virtual displays are always considered enabled
     mPowerMode = (mType >= DisplayDevice::DISPLAY_VIRTUAL) ?
                   HWC_POWER_MODE_NORMAL : HWC_POWER_MODE_OFF;
+#ifdef MTK_AOSP_ENHANCEMENT
+    mHwOrientation = DisplayState::eOrientationDefault;
+    mHwcMirrorId = -1;
+#endif
 
     // Name the display.  The name will be replaced shortly if the display
     // was created with createDisplay().
     switch (mType) {
         case DISPLAY_PRIMARY:
             mDisplayName = "Built-in Screen";
+#ifdef MTK_AOSP_ENHANCEMENT
+            switch (mFlinger->sPropertiesState.mHwRotation) {
+                case 90:
+                    mHwOrientation = DisplayState::eOrientation90;
+                    break;
+                case 180:
+                    mHwOrientation = DisplayState::eOrientation180;
+                    break;
+                case 270:
+                    mHwOrientation = DisplayState::eOrientation270;
+                    break;
+            }
+#endif
             break;
         case DISPLAY_EXTERNAL:
             mDisplayName = "HDMI Screen";
